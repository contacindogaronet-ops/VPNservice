<?xml version="1.0" encoding="utf-8"?>
<ScrollView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fillViewport="true">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="20dp">

        <TextView
            android:id="@+id/tv_rules_stats"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Rules Loaded: 0"
            android:textColor="#38BDF8"
            android:textSize="12sp"
            android:textStyle="bold"
            android:layout_marginBottom="10dp" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:layout_marginBottom="12dp">

            <Button
                android:id="@+id/btn_pick_rules"
                android:layout_width="0dp"
                android:layout_height="44dp"
                android:layout_weight="1"
                android:text="IMPORT RULES"
                android:textSize="11sp"
                android:textStyle="bold"
                android:backgroundTint="#6366F1"
                android:layout_marginEnd="6dp" />

            <Button
                android:id="@+id/btn_clear_rules"
                android:layout_width="0dp"
                android:layout_height="44dp"
                android:layout_weight="1"
                android:text="CLEAR"
                android:textSize="11sp"
                android:textStyle="bold"
                android:backgroundTint="#1E293B"
                android:layout_marginStart="6dp" />
        </LinearLayout>

        <androidx.cardview.widget.CardView
            android:layout_width="match_parent"
            android:layout_height="320dp"
            app:cardBackgroundColor="#131B2E"
            app:cardCornerRadius="14dp"
            app:cardElevation="0dp">

            <ScrollView
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:padding="14dp">

                <TextView
                    android:id="@+id/tv_rules_content"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="No rules active. All traffic is routed directly by Kernel."
                    android:textColor="#64748B"
                    android:fontFamily="monospace"
                    android:textSize="11sp" />
            </ScrollView>
        </androidx.cardview.widget.CardView>

    </LinearLayout>
</ScrollView>
